[alembic]
script_location = migrations
prepend_sys_path = .
version_path_separator = os
file_template = %%(rev)s_%%(slug)s

# URL подключения берётся из app.config (переменные окружения), см. migrations/env.py
