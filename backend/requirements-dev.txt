-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
aiosqlite==0.21.0
# Redis в памяти вместе с Lua: token bucket живёт в скрипте, и проверять
# его надо на настоящем интерпретаторе, а не на заглушке.
fakeredis[lua]==2.37.1
ruff==0.16.5
